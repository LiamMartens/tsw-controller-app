{
  "type": "object",
  "title": "Control Value Listener",
  "properties": {
    "type": {
      "type": "string",
      "enum": ["control_value"]
    },
    "actions": {
      "type": "array",
      "description": "The actions to perform when the control value changes and conditions are met",
      "items": {
        "$ref": "./profile.listener_action.schema.json"
      }
    }
  },
  "required": ["type", "name", "actions"]
}
